#![no_std]
#![deny(unsafe_code)]

#[cfg(ktest)]
mod tests {
    use kstd::prelude::*;

    #[ktest]
    fn it_works() {
        let memory_regions = &kstd::boot::boot_info().memory_regions;
        assert!(!memory_regions.is_empty());
    }
}
